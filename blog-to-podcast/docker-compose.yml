services:
  # ── API Gateway (điểm vào duy nhất cho browser/frontend) ──────────────
  api-gateway:
    build: ./services/api-gateway
    ports:
      - "8000:8000"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - CONTENT_SERVICE_URL=http://content-service:8001
      - PROCESS_SERVICE_URL=http://process-service:8002
      - TTS_SERVICE_URL=http://tts-service:8003
      - LIBRARY_SERVICE_URL=http://library-service:8005
      - NEWS_SERVICE_URL=http://news-service:8006
    depends_on:
      - content-service
      - process-service
      - tts-service
      - library-service
      - news-service
    volumes:
      - ./services/api-gateway/app:/app

  # ── Content Service (crawl URL) ────────────────────────────────────────
  content-service:
    build: ./services/content-service
    ports:
      - "8001:8001"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - USE_MOCK=${USE_MOCK:-false}
    volumes:
      - ./services/content-service/app:/app/app

  # ── Process Service (AI script via Claude AI Service) ──────────────────
  process-service:
    build: ./services/process-service
    ports:
      - "8002:8002"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - USE_MOCK=${USE_MOCK:-false}
      - AI_SERVICE_URL=http://ai-service:8004
      - AI_TIMEOUT=${AI_TIMEOUT:-180}
    depends_on:
      - ai-service
    volumes:
      - ./services/process-service/app:/app/app

  # ── AI Service (Anthropic/Claude) ──────────────────────────────────────
  ai-service:
    build: ./services/ai-service
    ports:
      - "8004:8004"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - ANTHROPIC_BASE_URL=${ANTHROPIC_BASE_URL:-https://1gw.gwai.cloud}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-claude-sonnet-4-6}
      - AI_TIMEOUT=${AI_TIMEOUT:-180}
      - AI_PROVIDER=claude
    volumes:
      - ./services/ai-service/app:/app/app

  # ── TTS Service (Google TTS / edge-tts) ───────────────────────────────
  tts-service:
    build: ./services/tts-service
    ports:
      - "8003:8003"
    dns:
      - 8.8.8.8
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - USE_MOCK=${USE_MOCK:-false}
      - AUDIO_DIR=${AUDIO_DIR:-audio_output}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - ./services/tts-service/app:/app/app
      - ./services/tts-service/audio_output:/app/audio_output

  # ── News Service (tìm bài báo theo chủ đề qua RSS) ──────────────────
  news-service:
    build: ./services/news-service
    ports:
      - "8006:8006"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - OPENWEATHER_API_KEY=${OPENWEATHER_API_KEY}
    volumes:
      - ./services/news-service/app:/app/app

  # ── Library Service (Lưu trữ podcast) ───────────────────────────────
  library-service:
    build: ./services/library-service
    ports:
      - "8005:8005"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    depends_on:
      - db
    volumes:
      - ./services/library-service/app:/app/app

  # ── Database (PostgreSQL) ──────────────────────────────────────────
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

networks:
  default:
    name: blog2cast-network
